# =============================================================================
# GENERATED FILE — DO NOT EDIT
# Source:    services.yml
# Generator: generator/render.py
# Regenerate: make generate
# =============================================================================
#
# WHAT THIS IS
#   Derived, non-secret variables: hostnames, ports and container names for
#   every registered service. Compose loads this alongside .env.
#
#   Split rationale — this file is generated and committed; .env is secret and
#   gitignored. Keeping them separate means hostnames stay reviewable in Git
#   while passwords never touch it (ADR-0006).
#
# WHY VALUES REFERENCE ${DOMAIN} INSTEAD OF EXPANDING IT
#   Expansion happens at container start, not at render time. The same
#   committed file is therefore correct for prod and dev; only ENV_PREFIX
#   differs. Nothing here hardcodes a hostname.

# ── Derived prefixes ────────────────────────────────────────────────────────
# Set by scripts/setup-env.sh from ENV:
#   ENV=prod -> ENV_PREFIX=""      ENV_PREFIX_NAME="prod-"
#   ENV=dev  -> ENV_PREFIX=".dev"  ENV_PREFIX_NAME="dev-"
# ENV_PREFIX      -> DNS infix    (n8n.dev.maxrommel.de)
# ENV_PREFIX_NAME -> object names (dev-n8n, dev-postgres, dev-net-data)

# ── n8n
# Workflow automation and integration engine
N8N_HOST=n8n${ENV_PREFIX}.${DOMAIN}
N8N_PORT=5678
N8N_CONTAINER=${ENV_PREFIX_NAME}n8n
N8N_HEALTHCHECK=/healthz
N8N_ENABLED=true

# ── paperclip
# Paperclip — the agent board (upstream, M11)
PAPERCLIP_HOST=paperclip${ENV_PREFIX}.${DOMAIN}
PAPERCLIP_PORT=3100
PAPERCLIP_CONTAINER=${ENV_PREFIX_NAME}paperclip
PAPERCLIP_HEALTHCHECK=/
PAPERCLIP_ENABLED=true

# ── status
# Uptime Kuma — service monitoring and alerting
STATUS_HOST=status${ENV_PREFIX}.${DOMAIN}
STATUS_PORT=3001
STATUS_CONTAINER=${ENV_PREFIX_NAME}status
STATUS_HEALTHCHECK=/
STATUS_ENABLED=true

# ── ntfy
# Push notification relay for alerts
NTFY_HOST=ntfy${ENV_PREFIX}.${DOMAIN}
NTFY_PORT=80
NTFY_CONTAINER=${ENV_PREFIX_NAME}ntfy
NTFY_HEALTHCHECK=/
NTFY_ENABLED=true

